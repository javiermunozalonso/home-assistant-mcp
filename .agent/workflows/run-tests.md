---
description: Run project tests using pytest and uv
---

Este workflow estandariza la ejecución de tests para asegurar que se usa el entorno correcto con `uv`.

1. Ejecuta todos los tests usando `uv run pytest`.
    // turbo

    ```bash
    uv run pytest
    ```
